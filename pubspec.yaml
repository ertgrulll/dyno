name: dyno
description: An isolation pool with load balancer for Flutter. Creates and kills isolates as needed.
version: 0.0.3
homepage: https://github.com/ertgrulll/dyno
repository: https://github.com/ertgrulll/dyno
issue_tracker: https://github.com/ertgrulll/dyno/issues
maintainer: Ertuğrul Yakın (@ertgrulll)

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter: